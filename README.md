### React 16 component unit tests.
Contains boilerplate for webpack-es6-react-jest stack setup.
Few examples of component unit tests.


```npm install```- install/update packages;

```npm test``` - run tests using Jest.